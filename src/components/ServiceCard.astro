---
interface Props {
  title: string;
  description: string;
  href: string;
}

const { title, description, href } = Astro.props;
---

<article class="card">
  <h2>{title}</h2>
  <p>{description}</p>
  <a href={href}>View service foundation</a>
</article>
