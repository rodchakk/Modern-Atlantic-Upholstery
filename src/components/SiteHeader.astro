---
import { PRIMARY_NAV, SITE } from '../config/site';

const currentPath = Astro.url.pathname;
const isActive = (href: string) =>
  href === '/' ? currentPath === '/' : currentPath.startsWith(href);
---

<header class="site-header">
  <div class="container header-inner">
    <a class="brand" href="/" aria-label={`${SITE.name} home`}>
      <span class="brand-name">Modern Atlantic</span>
      <span class="brand-sub">Upholstery · Matthews, NC</span>
    </a>

    <nav class="site-nav desktop-nav" aria-label="Primary navigation">
      <ul>
        {
          PRIMARY_NAV.map((item) => (
            <li>
              <a href={item.href} aria-current={isActive(item.href) ? 'page' : undefined}>
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>

    <div class="header-actions desktop-actions">
      <a class="header-phone" href={`tel:${SITE.phoneHref}`}>Call {SITE.phoneDisplay}</a>
      <a class="button button-small button-light" href="/contact/">Request a quote</a>
    </div>

    <details class="mobile-menu">
      <summary aria-label="Open navigation">Menu</summary>
      <nav aria-label="Mobile navigation">
        <ul>
          <li><a href="/">Home</a></li>
          {
            PRIMARY_NAV.map((item) => (
              <li>
                <a href={item.href} aria-current={isActive(item.href) ? 'page' : undefined}>
                  {item.label}
                </a>
              </li>
            ))
          }
        </ul>
        <a class="button button-light mobile-quote" href="/contact/">Request a quote</a>
      </nav>
    </details>
  </div>
</header>
